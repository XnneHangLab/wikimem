import{_ as s,o as i,c as t,a2 as a}from"./chunks/framework.BfRM_mob.js";const k=JSON.parse('{"title":"Retrieval","description":"","frontmatter":{},"headers":[],"relativePath":"guide/retrieval.md","filePath":"guide/retrieval.md","lastUpdated":1783734888000}'),n={name:"guide/retrieval.md"};function r(o,e,l,d,h,p){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="retrieval" tabindex="-1">Retrieval <a class="header-anchor" href="#retrieval" aria-label="Permalink to &quot;Retrieval&quot;">​</a></h1><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> index.retrieve(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;想去海边玩&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    limit</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># max search hits (before link expansion)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    budget_tokens</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">800</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># cap on injected content, None = uncapped</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    expand_links</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">True</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># one-hop wiki-link expansion</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    explain</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">False</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># True → keep what the budget dropped, for inspection</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>retrieve</code> is <strong>synchronous</strong>, makes <strong>zero LLM calls</strong>, and never raises for degraded optional paths — it is designed to sit directly on an agent&#39;s hot path, wrapped fail-open by the host. This page walks through the pipeline in execution order.</p><h2 id="_1-tokenization" tabindex="-1">1. Tokenization <a class="header-anchor" href="#_1-tokenization" aria-label="Permalink to &quot;1. Tokenization&quot;">​</a></h2><p>Queries and documents go through the same zero-dependency tokenizer:</p><ul><li>ASCII/latin runs → lowercased words (<code>[a-z0-9]+</code>).</li><li>CJK runs → <strong>character bigrams</strong> (&quot;海边玩&quot; → <code>海边</code>, <code>边玩</code>) — good keyword recall with no segmenter installed.</li><li>With the <code>[zh]</code> extra installed, CJK runs go through <strong>jieba</strong> instead. Detection is automatic; <code>MemoryIndex(store, use_jieba=...)</code> forces the path (<code>True</code> still falls back to bigrams when jieba is absent, <code>False</code> never uses it — useful for reproducible benchmarks).</li></ul><h2 id="_2-bm25-ranking" tabindex="-1">2. BM25 ranking <a class="header-anchor" href="#_2-bm25-ranking" aria-label="Permalink to &quot;2. BM25 ranking&quot;">​</a></h2><p>Every item is scored against the query with classic BM25 (<code>k1 = 1.5</code>, <code>b = 0.75</code>), over an index of the item&#39;s <strong>name + content</strong>. The index is:</p><ul><li><strong>In-memory only</strong> — never persisted; rebuilding from a few MB of markdown is effectively free at personal-memory scale.</li><li><strong>Lazily fresh</strong> — every <code>MemoryStore</code> write bumps a revision counter, and <code>retrieve</code> rebuilds the index if it is stale. Out-of-band file edits (your editor, <code>git pull</code>) are invisible to the counter — call <code>index.rebuild()</code> after those, or rely on the rebuild at process start.</li></ul><p>The top <code>limit</code> items by score become <strong>hits</strong>. If an <a href="/wikimem/guide/embedding-fusion.html">embedder is configured</a>, cosine scores are fused with BM25 at this stage — BM25 itself is never disabled.</p><h2 id="_3-one-hop-link-expansion" tabindex="-1">3. One-hop link expansion <a class="header-anchor" href="#_3-one-hop-link-expansion" aria-label="Permalink to &quot;3. One-hop link expansion&quot;">​</a></h2><p>With <code>expand_links=True</code> (the default), each hit&#39;s wiki-links are resolved by exact <code>(category, name)</code> lookup and the target items are appended <strong>directly after their hit</strong>, marked <code>source=&quot;link&quot;</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>hit   likes-the-sea      score=3.87   matched_terms=[&#39;海边&#39;, ...]</span></span>
<span class="line"><span>link  beach-trip-plan    via=&#39;likes-the-sea&#39;</span></span>
<span class="line"><span>hit   三亚之行            score=2.41   ...</span></span></code></pre></div><ul><li>Expansion is <strong>one hop</strong> and mechanical — no scoring, no LLM, no recursion (see <a href="/wikimem/guide/wiki-links.html#scope-one-hop-by-design">why</a>).</li><li>Duplicates are removed across the whole sequence: an item already present (as a hit or an earlier link target) is not injected twice.</li><li>Links whose target doesn&#39;t exist are skipped and reported in <code>result.unresolved_links</code>.</li></ul><h2 id="_4-token-budget" tabindex="-1">4. Token budget <a class="header-anchor" href="#_4-token-budget" aria-label="Permalink to &quot;4. Token budget&quot;">​</a></h2><p>The sequence is trimmed to <code>budget_tokens</code> with a <strong>prefix cut</strong>: entries are kept in injection order until the next one would exceed the budget. Two properties matter:</p><ul><li><strong>The first entry always survives</strong>, even if it alone exceeds the budget — retrieval never returns nothing because the best hit was long.</li><li>Costs use <code>est_tokens</code>, a deliberately crude estimate (one token per latin word, one per CJK character). It exists for <strong>stable trimming</strong>, not accuracy — don&#39;t reuse it for API billing math.</li></ul><p><code>result.budget_used</code> reports the estimated total of what survived.</p><h2 id="_5-reading-the-result" tabindex="-1">5. Reading the result <a class="header-anchor" href="#_5-reading-the-result" aria-label="Permalink to &quot;5. Reading the result&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> index.retrieve(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;咖啡&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">budget_tokens</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">800</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">explain</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">True</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result.items             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># list[RetrievedItem] — survived the budget, injection order</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result.budget_used       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># estimated tokens of the above</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result.embedding_used    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># True only when the cosine path actually ran</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result.unresolved_links  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># [&#39;[[daily_life:renamed-item]]&#39;, ...]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result.dropped           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># what the budget cut (explain=True only)</span></span></code></pre></div><p>Each <code>RetrievedItem</code> carries its evidence:</p><table tabindex="0"><thead><tr><th>field</th><th>meaning</th></tr></thead><tbody><tr><td><code>source</code></td><td><code>&quot;hit&quot;</code> (search match) or <code>&quot;link&quot;</code> (one-hop expansion)</td></tr><tr><td><code>score</code></td><td>ranking score — fused when embedding ran, else BM25</td></tr><tr><td><code>bm25_score</code> / <code>cos_score</code></td><td>the raw signals behind <code>score</code> (hits only)</td></tr><tr><td><code>via</code></td><td>for links: the hit item that pulled this in</td></tr><tr><td><code>matched_terms</code></td><td>query terms found in this item — the &quot;why&quot; of a hit</td></tr><tr><td><code>tokens_est</code></td><td>what this entry cost the budget</td></tr></tbody></table><p>This is design rule 4 in practice: a host can log, display, or debug exactly why each memory reached the prompt.</p><h2 id="edge-cases-worth-knowing" tabindex="-1">Edge cases worth knowing <a class="header-anchor" href="#edge-cases-worth-knowing" aria-label="Permalink to &quot;Edge cases worth knowing&quot;">​</a></h2><ul><li><strong>Empty query terms</strong> (e.g. punctuation-only query) → empty result, unless the embedding path is active — cosine can still rank what BM25 cannot.</li><li><strong><code>add</code> of an existing name is an update</strong> — the index picks it up via the revision counter on the next <code>retrieve</code>.</li><li><strong>Scores are corpus-relative.</strong> BM25 depends on document frequencies, and fused scores are min-max normalized per query — compare within one result, not across queries or stores.</li></ul>`,25)])])}const u=s(n,[["render",r]]);export{k as __pageData,u as default};
