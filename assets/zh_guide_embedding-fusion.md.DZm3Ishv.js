import{_ as e,o as i,c as a,a2 as n}from"./chunks/framework.BWuWLRhz.js";const c=JSON.parse('{"title":"Embedding 融合","description":"","frontmatter":{},"headers":[],"relativePath":"zh/guide/embedding-fusion.md","filePath":"zh/guide/embedding-fusion.md","lastUpdated":1783734888000}'),t={name:"zh/guide/embedding-fusion.md"};function d(l,s,o,p,r,h){return i(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="embedding-融合" tabindex="-1">Embedding 融合 <a class="header-anchor" href="#embedding-融合" aria-label="Permalink to &quot;Embedding 融合&quot;">​</a></h1><p>BM25 匹配词面。有时你要的是含义：&quot;海滨度假&quot;应该召回写着&quot;喜欢海边&quot;的条目， 哪怕两者一个字都不重合。这就是可选的 <code>[embed]</code> extra 增加的东西 —— <strong>也只有</strong>这个：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;wikimem[embed]&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 增加 httpx + numpy</span></span></code></pre></div><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> wikimem </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MemoryIndex, MemoryStore</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> wikimem.vectors </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HttpEmbedder</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">store </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MemoryStore(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;memory/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">embedder </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HttpEmbedder(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://api.example.com/v1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bge-m3&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">api_key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sk-…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">index </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MemoryIndex(store, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">embedder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">embedder)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> index.retrieve(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;海滨度假&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 与&quot;喜欢海边&quot;零词面重合也能召回</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result.embedding_used)          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># False = 端点不可用，BM25 照常工作</span></span></code></pre></div><p>不传 <code>embedder</code> → 整个模块根本不会被导入。零依赖内核保持完整。</p><h2 id="bm25-从不关闭" tabindex="-1">BM25 从不关闭 <a class="header-anchor" href="#bm25-从不关闭" aria-label="Permalink to &quot;BM25 从不关闭&quot;">​</a></h2><p>配置 embedder 后，<strong>每次查询两路信号都跑</strong>：</p><ol><li>BM25 照常给所有条目打分。</li><li>query 被 embed，对向量索引做余弦打分。</li><li>两组分数各自在候选并集上 <strong>min-max 归一</strong>，然后融合： <code>score = w · bm25 + (1 − w) · cos</code>，<code>w = fusion_weight</code> （默认 <code>0.5</code> —— 与 memU ADR-0007 收敛到的混合公式一致）。</li></ol><p>BM25 抓词面、余弦抓含义，谁都不会被悄悄丢掉：只被一路信号找到的条目照样进入 候选集。关键词为主的场景把 <code>fusion_weight</code> 往 <code>1.0</code> 调， 改写/同义表达为主的场景往 <code>0.0</code> 调。</p><h2 id="永远-fail-open" tabindex="-1">永远 fail-open <a class="header-anchor" href="#永远-fail-open" aria-label="Permalink to &quot;永远 fail-open&quot;">​</a></h2><p>embedding 端点是网络依赖，wikimem 拒绝让它成为单点故障：</p><ul><li>端点宕机、超时、密钥错误、响应异常 —— 余弦路径直接返回空，检索<strong>静默降级为 纯 BM25</strong>，<code>result.embedding_used</code> 为 <code>False</code>。<code>retrieve</code> 绝不因端点不可用 而抛错。</li><li>判定是逐 query 的。端点恢复后，下一个 query 自动恢复融合 —— 没有需要复位的熔断器。</li></ul><p>想知道融合路径实际跑了多少，在宿主日志里观察 <code>embedding_used</code>。</p><h2 id="向量缓存" tabindex="-1">向量缓存 <a class="header-anchor" href="#向量缓存" aria-label="Permalink to &quot;向量缓存&quot;">​</a></h2><p>BM25 索引在启动时免费重建，向量不一样 —— 重算要花 embedding API 的钱。 所以向量放进一个<strong>持久、增量更新的缓存</strong>，就在 markdown 旁边， 并享有和其他派生状态相同的保证：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>memory/</span></span>
<span class="line"><span>├── preferences.md</span></span>
<span class="line"><span>├── daily_life.md</span></span>
<span class="line"><span>├── journal.jsonl</span></span>
<span class="line"><span>├── vectors-000003.npy     ← float32 矩阵，一行一个条目</span></span>
<span class="line"><span>└── vectors.keys.jsonl     ← 明文：哪一行对应哪个条目（内容哈希）</span></span></code></pre></div><ul><li><strong>按内容哈希建键。</strong> 每次索引重建只 embed 新增或变化的条目（每批 64 条）， 未变的行直接复用、不发 API 请求。改个条目名，也只重新 embed 那一条。</li><li><strong>该可读的地方是可读的。</strong> <code>vectors.keys.jsonl</code> 是纯 JSONL —— 头一行指明 当前 <code>.npy</code>，之后一行一条 <code>{category, name, hash}</code>。矩阵本身是数字， 但<em>谁对应谁</em>永远是文本。</li><li><strong>随时可删。</strong> 两个文件删掉，下次 sync 自动重建。它永远不是事实源。</li><li><strong>版本化的 <code>.npy</code></strong>（<code>vectors-000001.npy</code>、<code>-000002.npy</code>、……）：Windows 不允许替换仍被活索引 memory-map 的文件，所以每次 sync 写新版本、 尽力清理旧版本，清不掉的留给后续 sync。keys 与矩阵对不上的&quot;撕裂&quot;状态 按&quot;缓存不存在&quot;处理并重建 —— 绝不信任损坏数据。</li></ul><p>想把缓存放到别处（比如挪出同步盘），用 <code>MemoryIndex(store, embedder=..., vectors_dir=&quot;…&quot;)</code>。</p><h2 id="内存故事-memmap-分层" tabindex="-1">内存故事：memmap 分层 <a class="header-anchor" href="#内存故事-memmap-分层" aria-label="Permalink to &quot;内存故事：memmap 分层&quot;">​</a></h2><p>全精度向量从不全量驻留内存：</p><ul><li><strong>第 0 层 —— 不超过 <code>binary_threshold</code> 条（默认 10 000）：</strong> 对 float32 <strong>memmap</strong> 做暴力余弦。哪些页驻留由操作系统页缓存决定；个人记忆规模下 是微秒级。</li><li><strong>第 1 层 —— 超过阈值：</strong> 紧凑的 1-bit 签名（768 维下每条 96 字节）驻留 内存做 Hamming 距离粗排；只有前 <code>k × 4</code> 个候选行从 memmap 读回， 做精确余弦重排。</li></ul><p>切换是自动的、按构建时条数判定；<code>binary_threshold</code> 是 <code>MemoryIndex</code> 的构造参数，精度/内存的取舍不同就调它。</p><h2 id="自带-embedder-——-或自带索引" tabindex="-1">自带 embedder —— 或自带索引 <a class="header-anchor" href="#自带-embedder-——-或自带索引" aria-label="Permalink to &quot;自带 embedder —— 或自带索引&quot;">​</a></h2><p>两个小 protocol 让整层可插拔（<a href="/zh/reference/vectors.html">参考</a>）：</p><ul><li><strong><code>Embedder</code></strong> —— 任何实现 <code>embed(texts: list[str]) -&gt; list[list[float]]</code> 的对象。<code>HttpEmbedder</code> 覆盖一切 OpenAI 兼容的 <code>/embeddings</code> 端点（OpenAI、SiliconFlow、Ollama、 vLLM、……）；包一个本地 sentence-transformers 也就五行。</li><li><strong><code>VectorIndex</code></strong> —— 检索端口：<code>search(query, top_k) -&gt; [(row, score)]</code>。 内置 <code>MemmapVectorIndex</code> 是默认后端；更重的后端（sqlite-vec、Qdrant local、……）适配到同一接口后面，检索代码一行不改。 （接口借鉴 mem0 的 VectorStore 抽象 —— 借端口，不借默认后端。）</li></ul>`,25)])])}const m=e(t,[["render",d]]);export{c as __pageData,m as default};
